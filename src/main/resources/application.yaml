form-flow:
  path: 'flows-config.yaml'
spring:
  thymeleaf:
    cache: false
  datasource:
    url: jdbc:postgresql://localhost:5432/formflow
management:
  endpoints:
    web:
      exposure:
        include: '*'